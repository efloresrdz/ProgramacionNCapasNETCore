@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>
@{
    ViewData["Title"] = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container">
    <div class=row>
        <div class="text-center">
            <h2>Usuarios</h2>

        </div>
        <div class="col-md-6 col-sm-12">
            <h2>Detalle</h2>

        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <h5>Seleccione un Usuario para editarlo o + para agregar uno nuevo...</h5>
            <hr />
        </div>
    </div>

    <div style="float:right;">
        @Html.ActionLink("+", "Form", "UsuarioController", htmlAttributes: new { @class = "btn btn-success" })
    </div>

    @if (Model != null)
    {
        <div class="row">
            <table class="table table-hover table-striped table-bordered table-responsive" id="table_usuarios">
                <thead>
                    <tr class="table-secondary">
                        <td>Editar</td>
                        <td style="display :none;">id</td>
                        <td>Nombre</td>
                        <td>Eliminar</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var users in Model)
                    {
                        <tr>
                            <td>
                                <form asp-action="Form" asp-route-id="@users.Id" method="get">
                                    <button type="submit" class="btn btn-sm btn-warning">
                                        Actualizar
                                    </button>
                                </form>
                            </td>
                            <td style="display :none;">@users.Id </td>
                            <td>@users.UserName</td>

                            <td>
                                <form asp-action="Delete" asp-route-id="@users.Id" method="post">
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        Delete
                                    </button>
                                </form>
                            </td>
                            @* <td style="text-align: center; vertical-align: middle;"><a class="btn btn-danger glyphicon glyphicon-trash"  href="@Url.Action("Delete", "RoleController", new { IdRole = role.Id})" onclick="return confirm('Estas seguro que deseas eliminar este registro?');"><i class="bi bi-eraser-fill" style="color: mintcream"></i></a></td>*@
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

</div>